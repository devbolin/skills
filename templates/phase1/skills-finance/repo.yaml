# Deprecated compatibility file.
# Source of truth is pack.yaml in the same directory.
repo_id: finance-skills
name: Finance Skills
summary: finance domain skill pack
owners:
  - finance-platform
visibility: internal

defaults:
  release_channel: stable
  permissions:
    network: false
  connectors:
    - storage:read

shared:
  prompts_path: shared/prompts
  libs_path: shared/libs
  tests_path: shared/tests

skills:
  - id: invoice-extractor
    path: skills/invoice-extractor
